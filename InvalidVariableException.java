public class InvalidVariableException extends Exception {
    // Constructor for custom exception class
    public InvalidVariableException(String message) {
        // Sends the error message to the parent Exception class
        super(message);
    }
}
